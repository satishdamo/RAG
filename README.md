# RAG Backend API

A backend service that powers **Retrieval-Augmented Generation (RAG)** applications.  
This API provides endpoints for document ingestion, retrieval, and generation using LLMs combined with a vector database.

---

## 🚀 Features

- Document ingestion and preprocessing (PDF, TXT, DOCX, etc.)
- Embedding generation using transformer-based models
- Vector storage and similarity search
- RAG pipeline: retrieve relevant context + generate response
- RESTful API endpoints for integration with frontend or other services
- Configurable model and database backends

---
